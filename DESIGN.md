# DESIGN.md — myblog 디자인 시스템

블로그 정체성: **학부생의 CS 학습 정리 노트**. 연구 노트/실험 노트의 여백 메모(marginalia) 감각을 미니멀하게.
이 문서가 모든 시각 결정의 단일 출처다. 컴포넌트는 여기 정의된 CSS 변수만 사용한다.

## 색 (light)
| 토큰 | 값 | 용도 |
|------|-----|------|
| `--paper`  | `#FCFCFA` | 배경(차분한 종이색, 크림 아님) |
| `--ink`    | `#1A1A1F` | 본문 텍스트 |
| `--muted`  | `#6B6B76` | 여백 메타(날짜·태그), 보조 텍스트 |
| `--rule`   | `#E6E6E1` | 얇은 구분선(절제 사용) |
| `--accent` | `#2F54EB` | 링크·강조(볼펜 잉크 블루) |
| `--marker` | `rgba(120,224,164,0.45)` | 형광펜 마커(호버 상호작용) |
| `--margin-line` | `#E7C3BE` | 노트 여백선(흐린 빨강, 시그니처) |
| `--grid` | `rgba(26,26,31,0.035)` | 모눈종이 배경선(아주 흐리게) |

## 색 (dark, `[data-theme="dark"]`)
| 토큰 | 값 |
|------|-----|
| `--paper`  | `#16161A` |
| `--ink`    | `#ECECEF` |
| `--muted`  | `#9A9AA6` |
| `--rule`   | `#2A2A31` |
| `--accent` | `#6B8AFF` |
| `--marker` | `rgba(108,231,150,0.22)` |
| `--margin-line` | `#5A3A3C` |
| `--grid` | `rgba(255,255,255,0.04)` |

## 타이포그래피
- 출처: Pretendard(jsDelivr 가변·동적 서브셋) + Google Fonts(Gowun Batang·JetBrains Mono).
- **Display**(사이트 제목·글 제목·목록 제목·브레드크럼): `"Gowun Batang", serif` — 국문 세리프, 노트 정체성. "marquee" 제목에만.
- **Body**(본문·UI): `"Pretendard Variable", -apple-system, system-ui, sans-serif` — 한국 개발 블로그 표준, 화면 가독성.
- **글 안 소제목**(`.post-body` h2/h3): Body(Pretendard) 700, 자간 -0.01em — 코드 많은 글의 스캔성. 세리프는 글 *제목*까지만.
- **Mono**(코드·날짜·개수): `"JetBrains Mono", monospace`.
- 스케일(rem): h1 2.0 / h2 1.5 / h3 1.2 / body 1.075 / small 0.85.
- 본문 측정폭(measure): 최대 `72ch`. 행간 1.75.

## 레이아웃 & 시그니처
- **시그니처 = 노트 여백선(margin rule)**: 학생 노트의 빨간 세로 여백선(`--margin-line`, 1px). 날짜를 이 선에 **오른쪽 정렬로 매달아** 인덱스·글 페이지 양쪽에 동일하게 적용 → 두 화면이 같은 시각 언어를 공유한다. 선은 `linear-gradient` 1px 세로 배경으로 그린다(요소 보더 아님).
  - **인덱스/목록**: `[date 5rem] · 여백선 · [content]` 그리드. 날짜는 모노·우측 정렬로 선에 붙는다. <33rem에서 1열로 접힘(선 제거).
  - **글 페이지**: ≥1024px일 때 `[rail 12rem] · 여백선 · [content]`, gap 2.5rem, 중앙 정렬. rail에 날짜·태그를 우측 정렬 메모로. <1024px에서는 본문 위로 접힘.
- **하이라이터 마커**: 링크 호버·활성 태그에 `--marker` 밑줄형 배경(120ms 확장). 구조 시그니처(여백선)와 별개의 *상호작용* 시그니처로 조용히 유지.
- **모눈종이 배경**: 전체 배경에 28px 모눈을 `linear-gradient` 2겹으로(가로·세로) 깔아 노트 표면 질감을 준다. `--grid`로 아주 흐리게(본문 대비 유지). 코드블록은 `--paper` 단색 배경으로 덮어 모눈이 비치지 않게.
- **마스트헤드(홈)**: 노트 표지 — 모노 eyebrow + 세리프 대형 제목(형광펜 마커를 정적으로 깔아 히어로 포인트) + 한 줄 정체성 + 모노 원장(ledger) 줄(기록 수). 제목 = 학습 노트의 thesis.
- **eyebrow 라벨**: 섹션 상단 모노·대문자·자간 영문 소라벨(LAB NOTEBOOK / INDEX / TAG / COLOPHON)로 노트 라벨 질감. 짧고 드물게.
- **카테고리(다계층 경로 `A/B/C…`, 이름엔 `/` 금지·구분자)**: 별도 탭 없이 **게시글 좌측 INDEX**로 노출(`.index-layout` 2열, ≥768px sticky). 트리는 글 있는 가지만 자동 생성(빈 가지는 글 생기면 등장), 재귀 렌더(`CatTree`, `--margin-line` 좌측 보더로 깊이 들여쓰기, 깊을수록 muted, 개수 모노), 현재 위치는 accent 밑줄. 형제 순서는 `CATEGORY_ORDER` 우선(없으면 한글 정렬). 노드 클릭 시 그 서브트리 글 전체를 우측에 + 브레드크럼. 글 레일엔 경로 브레드크럼. 태그(교차 분류)와 별개로 카테고리는 글당 하나의 위치 분류.
- border-radius: 2px 이하(거의 각짐). 그림자 없음. 구분선은 헤어라인만.
- 색 근거: 빨강 여백선 + 파랑 볼펜(accent) + 초록 형광펜(marker) = 실제 학생 노트. 장식이 아니라 주제에서 온 선택. 빨강은 종이 인쇄 느낌으로 흐리게.

## 모션
- 글 본문: 로드 시 8px 상승 + 페이드(150ms). 그 외 페이지 전환 애니메이션 없음.
- 태그/링크 호버: 마커 배경 120ms 확장.
- `prefers-reduced-motion: reduce`에서 모든 모션 제거.

## 접근성 바닥선
- 본문 대비 ≥ 7:1(`--ink`/`--paper` 충족). 모바일 1열 반응형.
- `:focus-visible` 아웃라인 `2px solid var(--accent)`.
- 다크/라이트 토글은 시스템 설정 기본값 + 수동 전환(localStorage 저장).
