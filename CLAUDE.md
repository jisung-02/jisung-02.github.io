# CLAUDE.md

이 저장소의 작업 규칙은 [AGENTS.md](./AGENTS.md)에 있다 — 시작 전 반드시 읽을 것. (카테고리=폴더 경로, URL=슬러그화 경로, 링크 수동 갱신, 첨부 상대경로 보정, 한글·공백 경로 일괄수정은 Python으로.)

Claude 전용 메모:
- 콘텐츠 대량 변경(파일명·삭제 등)은 실행 후 **무엇을 바꿨는지 요약 보고**하고, 사용자가 커밋 전 검토한다.
- 구조/링크를 건드렸으면 커밋 전 `npm run build`로 검증(깨진 링크 0).
- 명시 요청 없이는 커밋·푸시하지 않는다.
