# jisung-02 블로그 디자인

개인 프로젝트와 공부 내용을 읽기 편하게 보여 주는 블로그. 시각 결정의 기준은 `src/styles/global.css`다.

- 배경 `#fff`, 본문 `#242424`, 보조 텍스트 `#717171`, 구분선 `#e7e7e7`, 강조 `#b74d21`.
- 다크 모드는 같은 구조에 어두운 중성색과 `#e5a276` 강조색을 사용한다. 시스템 설정을 기본으로, 사용자의 선택을 로컬에 저장한다.
- Inter와 Noto Sans KR을 사용하고, 로드되지 않으면 시스템 글꼴로 표시한다.
- 제목과 본문은 산세리프. 그라데이션과 장식적인 소개 문구 대신 제목·날짜·분류를 중심으로 구성한다.
- 홈은 GitHub 프로필, 대표 글 3개, 분류별 모아보기, 최신 글 순서다. `featured: true`인 글을 대표 글로 우선 배치한다.
- 헤더는 스크롤을 따라오며 Posts / Archive / About, 검색, 테마 버튼을 제공한다. 680px 이하에서는 Menu로 접힌다.
- 검색은 제목·설명·본문·태그에서 공백으로 나눈 검색어를 모두 포함하는 글을 찾는다. 한글 유니코드 정규화를 지원한다. Cmd/Ctrl+K로 검색창에 접근한다.
- 글 목록은 날짜와 제목을 정렬하고, 작은 화면에서는 한 열로 접힌다. 분류 인덱스는 800px 이하에서 본문 위에 놓인다. 대표 글은 430px 이하에서 한 열로 표시한다.
- 글 페이지는 읽기 폭을 제한하며 넓은 화면에는 목차를 표시한다. 코드·표·긴 수식은 가로 스크롤을 허용해 페이지 전체가 넘치지 않도록 한다.
- 키보드 포커스와 reduced-motion을 지원한다. 모바일 메뉴는 Escape와 바깥 클릭으로 닫힌다.

## 홈 화면 마감
분류와 최신 글은 같은 860px 기준선에 맞춘다. 분류 링크는 별도 상자 대신 얇은 밑줄로 구분하고, 글 제목과 보조 태그의 굵기·크기를 구분한다. 긴 카드 제목은 균형 있게 줄바꿈하며 카드 호버 이동은 2px로 제한한다.

## 분류 사이드바
Archive 제목 아래 콘텐츠 영역을 왼쪽 분류 목록과 오른쪽 검색·글 목록의 2열로 구성한다. 분류 목록은 해당 영역 안에서만 sticky로 따라오며 화면 가장자리에 고정하지 않는다. 800px 이하에서는 분류 목록을 글 목록 위에 배치한다. 상단 메뉴는 일반 헤더를 유지한다.
