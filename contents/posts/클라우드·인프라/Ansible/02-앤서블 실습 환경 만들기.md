---
title: "Ansible 학습"
date: 2026-03-30
publish: true
tags: ["Ansible", "인프라"]
description: "Ansible 소개와 특징(에이전트리스·멱등성 등) 정리"
---
> 참고자료
> 1. https://product.kyobobook.co.kr/detail/S000209345794


# 앤서블 실습 환경 만들기
- 현재 나의 로컬 환경은 M2 Mac이므로 이를 기준으로 한다.

## 가상환경 설치
[UTM 설치](../가상머신/UTM%20설치.md)
- 앤서블이 실행될 가상환경을 생성한다.

## 앤서블 설치
우분투 환경 기준:
```bash
sudo apt update
sudo apt install software-properties-common
# PPA(add-apt-repository)를 사용하기 위한 도구 설치
sudo add-apt-repository --yes --update ppa:ansible/ansible
# Ansible 공식 PPA 저장소를 APT에 추가 + 즉시 업데이트
sudo apt install ansible
```

- PPA (Personal Package Archive): 공식 저장소가 아닌, 패키지가 배포되는 팀/개인용 저장소
- `sudo add-apt-repository --yes --update ppa:ansible/ansible`이 하는 일:
	1. PPA URL 변환
	2. `/etc/apt/sources.list.d/`에 파일 생성
	3. GPG key 등록
	4. `apt update` 수행

macOS 기준:
```bash
brew update
brew install ansible
```


